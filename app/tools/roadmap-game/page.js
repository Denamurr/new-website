import RoadmapGameClient from '../../../components/RoadmapGameClient'

export const metadata = {
  title: 'PM Survival Game — denamurr.com',
  description: 'A PM decision-making game. Five sprints, five stats, no good options.',
}

export default function RoadmapGamePage() {
  return <RoadmapGameClient />
}
